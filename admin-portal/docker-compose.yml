services:
  quadtv-admin:
    build: .
    container_name: quadtv-admin
    restart: unless-stopped
    env_file: .env
    ports:
      - "8088:8088"
    volumes:
      - quadtv-admin-data:/app/data
volumes:
  quadtv-admin-data:
